<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CMS Form</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
        }

        /* Navbar Styling */
        .navbar {
            background-color: #333;
            padding: 15px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .navbar .logo {
            color: white;
            font-size: 24px;
            font-weight: bold;
            text-transform: uppercase;
        }

        .navbar ul {
            list-style: none;
            display: flex;
            gap: 20px;
        }

        .navbar ul li {
            display: inline;
        }

        .navbar ul li a {
            color: white;
            text-decoration: none;
            font-size: 18px;
            padding: 10px 15px;
            transition: background 0.3s ease;
        }

        .navbar ul li a:hover {
            background-color: #575757;
            border-radius: 5px;
        }

        /* Form Container */
        .form-container {
            max-width: 600px;
            margin: 40px auto;
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .form-header {
            text-align: center;
            margin-bottom: 20px;
        }

        .form-header h1 {
            font-size: 28px;
            margin-bottom: 10px;
        }

        .form-header h4 {
            font-size: 18px;
            color: #555;
            margin-bottom: 10px;
        }

        .form-header p {
            font-size: 16px;
            color: #777;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        form label {
            margin-bottom: 5px;
            font-weight: bold;
        }

        form input, form textarea {
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
        }

        form input[type="submit"] {
            background-color: #333;
            color: white;
            border: none;
            cursor: pointer;
            font-size: 18px;
        }

        form input[type="submit"]:hover {
            background-color: #575757;
        }

        /* Responsive for smaller screens */
        @media screen and (max-width: 768px) {
            .navbar ul {
                flex-direction: column;
                gap: 10px;
            }

            .form-container {
                padding: 15px;
            }
        }
    </style>
</head>
<body>

    <!-- Navbar -->
    <%-include("./components/navbar.ejs")%>

    <!-- CMS Form Container -->
    <div class="form-container">
        <div class="form-header">
            <h1>Edit Post</h1>
            <h4>Enter the details below</h4>
            <p>Fill in the title, subtitle, and description to add a new post to the CMS.</p>
        </div>

        <form action="/editBlog1/<%-data[0].id%>" method="post">
            <label for="title">Title</label>
            <input type="text" id="title" name="title" value="<%-data[0].title%>" required>

            <label for="subtitle">Subtitle</label>
            <input type="text" id="subtitle" name="subtitle" value="<%-data[0].subTitle%>" required>

            <label for="description">Description</label>
            <textarea id="description" name="description" rows="4" required><%-data[0].description%>
            </textarea>

            <input type="submit" value="UPDATE">
        </form>
    </div>

</body>
</html>
